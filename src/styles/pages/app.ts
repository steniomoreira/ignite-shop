import { styled } from "..";

export const Container = styled('div', {
    position: "relative",
    display: "flex",
    flexDirection: 'column',
    alignItems: 'flex-start',
    justifyContent: 'center',
    minHeight: '100vh',
    
    overflow: "hidden",
})